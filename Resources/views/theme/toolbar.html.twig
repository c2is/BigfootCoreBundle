<!-- Navigation starts -->
<div class="nav-collapse collapse">

    <!-- Links -->
    {% if user_menu.items|length %}
    <ul class="nav pull-right">
        <li class="dropdown pull-right">
            <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                <span class="glyphicon glyphicon-user"></span> {{ app.security.token.user.name }} <b class="caret"></b>
            </a>

            <!-- Dropdown menu -->
            <ul class="dropdown-menu">
                {% for item in user_menu.items %}
                <li><a href="{{ path(item.route, item.parameters) }}"><span class="glyphicon glyphicon-{{ item.icon }}"></span> {{ item.label }}</a></li>
                {%  endfor %}
            </ul>
        </li>
    </ul>
    {% endif %}
</div>
